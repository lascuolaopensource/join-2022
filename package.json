{
  "name": "root",
  "private": true,
  "engines": {
    "node": "^16.12"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:shared": "yarn workspace join-shared build",
    "build:backend": "yarn workspace join-backend build",
    "start:backend": "yarn workspace join-backend start",
    "build:frontend": "yarn workspace join-frontend build",
    "turboBuild": "turbo build",
    "dev": "lerna run dev --parallel",
    "clean": "lerna clean",
    "types": "yarn workspace join-shared types"
  },
  "devDependencies": {
    "lerna": "^6.0.3",
    "turborepo": "^0.0.1"
  }
}
