<script lang="ts">
	import { getContext } from 'svelte';
	import { key } from './key';

	// const { isValid } = getContext(key);
</script>

<!--  -->

<button type="submit" class="btn btn-primary">
	<slot />
</button>
